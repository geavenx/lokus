[project]
name = "swagger-validator-v2"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyyaml>=6.0.2",
    "reportlab>=4.4.1",
    "typer>=0.15.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
